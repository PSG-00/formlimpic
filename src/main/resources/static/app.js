const app=document.querySelector('#app');let timer,offset=0;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const date=s=>new Date(s).toLocaleString('ko-KR',{timeZone:'Asia/Seoul',hour12:false});
const time=s=>{const d=new Date(s);return d.toLocaleTimeString('en-GB',{timeZone:'Asia/Seoul',hour12:false})+'.'+String(d.getUTCMilliseconds()).padStart(3,'0')};
function toast(s){const t=document.querySelector('#toast');t.textContent=s;t.style.display='block';setTimeout(()=>t.style.display='none',6000)}
async function api(path,method='GET',body){const r=await fetch('/api'+path,{method,headers:{'Content-Type':'application/json','X-Formlimpic':'formlimpic'},body:body?JSON.stringify(body):undefined});const data=await r.json();if(!r.ok)throw Error(data.message||'요청에 실패했습니다. 다시 시도해주세요.');if(data.serverTime)offset=Date.parse(data.serverTime)-Date.now();return data}
const now=()=>Date.now()+offset;
function phase(f){return now()>=Date.parse(f.expiresAt)?'결과 공개':now()>=Date.parse(f.startsAt)?'접수 중':now()>=Date.parse(f.startsAt)-600000?'미리 작성 가능':'오픈 예정'}
async function route(){clearInterval(timer);try{const [kind,id]=location.hash.slice(1).split('/');if(kind==='new')return create();if(kind==='form')return await detail(id);if(kind==='submit')return await wizard(id);await home()}catch(e){toast(e.message);app.innerHTML='<div class="empty">화면을 불러오지 못했습니다. <a href="#">홈으로</a></div>'}}
async function home(){const data=await api('/forms');app.innerHTML=`<section class="hero"><div><div class="eyebrow">THE PERFECT TIMING</div><h1>정각의 순간,<br>폼림픽으로 연습하세요.</h1><p>미리 작성하고, 원하는 순간에 제출하세요.<br>접수 시간부터 최종 순위까지 한눈에 확인할 수 있어요.</p></div><div class="hero-art">⏱</div></section><div class="notice">연습용 서비스입니다. 이름과 연락처에는 반드시 가상 정보를 입력하세요.</div><div class="section-head"><h2>열려 있는 폼림픽 <span class="muted">${data.forms.length}</span></h2><a class="button" href="#new">＋ 폼림픽 만들기</a></div><div class="grid">${data.forms.slice().reverse().map(f=>`<a class="card" href="#form/${f.id}"><span class="badge">${phase(f)}</span><h2>${esc(f.title)}</h2><p class="muted">신청 ${date(f.startsAt)}<br>마감 ${date(f.expiresAt)}</p><span class="small">폼림픽 확인하기 ↗</span></a>`).join('')}</div>${data.forms.length?'':'<div class="empty">아직 폼림픽이 없어요. 첫 연습을 열어보세요.</div>'}`}
function create(){app.innerHTML=`<div class="panel"><a class="back" href="#">← 목록으로</a><h1>폼림픽 만들기</h1><p>신청 시작 10분 전부터 폼을 작성할 수 있어요.</p><form id="create"><label class="field"><span>제목</span><input name="title" maxlength="120" required placeholder="예: 5시 정각 폼림픽 연습"></label><label class="field"><span>내용</span><textarea name="content" maxlength="10000" required placeholder="폼림픽 안내를 적어주세요."></textarea></label><div class="dates"><label class="field"><span>신청 시작 · 기기 현지 시간</span><input name="start" type="datetime-local" step="1" required></label><label class="field"><span>만료 · 기기 현지 시간</span><input name="end" type="datetime-local" step="1" required></label></div><div class="notice">정각 이전 제출은 정상 신청자 뒤에 배정됩니다. 만료 후 이름·멤버십 코드·순위·접수 시각이 공개됩니다.</div><button>폼림픽 생성하기</button></form></div>`;document.querySelector('#create').onsubmit=async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;const f=new FormData(e.target);try{const r=await api('/forms','POST',{title:f.get('title'),content:f.get('content'),startsAt:new Date(f.get('start')).toISOString(),expiresAt:new Date(f.get('end')).toISOString()});location.hash='form/'+r.id}catch(x){toast(x.message);b.disabled=false}}}
async function detail(id){const d=await api('/forms/'+id),f=d.form;app.innerHTML=`<div class="panel"><a class="back" href="#">← 전체 폼림픽</a><p><span class="badge" id="phase">${phase(f)}</span></p><h1>${esc(f.title)}</h1><div class="content">${esc(f.content)}</div><hr><p>신청 시작 ${date(f.startsAt)}<br>만료 ${date(f.expiresAt)}<br><span class="small">한국 시간(KST) 기준</span></p><div class="muted">서버 시각 추정 · 실제 판정은 서버 접수 시각 기준</div><div class="clock" id="clock"></div><div class="notice">실제 개인정보를 입력하지 마세요. 조기 제출은 정상 신청자 뒤로 배정됩니다.</div><button id="enter"></button><section id="mine"></section><section id="results"></section></div>`;const b=document.querySelector('#enter');b.onclick=()=>{if(d.mine)showMine(d.mine);else location.hash='submit/'+id};let loaded=false;
const tick=async()=>{if(!document.querySelector('#clock'))return;document.querySelector('#clock').textContent=time(now());document.querySelector('#phase').textContent=phase(f);b.textContent=d.mine?'나의 답변 확인하기':now()>=Date.parse(f.expiresAt)?'신청 마감':'폼 제출';b.disabled=!d.mine&&(now()<Date.parse(f.startsAt)-600000||now()>=Date.parse(f.expiresAt));if(!loaded&&now()>=Date.parse(f.expiresAt)){loaded=true;try{const rows=await api('/forms/'+id+'/results');document.querySelector('#results').innerHTML=`<h2 class="top-space">최종 순위</h2><p class="small">같은 밀리초의 신청은 서버 내부 접수 순번으로 구분합니다.</p><div class="table-wrap"><table><thead><tr><th>순위</th><th>이름</th><th>멤버십 코드</th><th>접수 시각(KST)</th><th>구분</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.rank}</td><td>${esc(r.name)}</td><td>${r.membershipCode}</td><td title="${esc(date(r.receivedAt))}">${time(r.receivedAt)}</td><td>${r.early?'조기 제출':'정상'}</td></tr>`).join('')}</tbody></table></div>${rows.length?'':'<p>제출된 답변이 없습니다.</p>'}`}catch(e){loaded=false}}};tick();timer=setInterval(tick,100)}
function showMine(r){document.querySelector('#mine').innerHTML=`<h2 class="top-space">나의 답변</h2><dl class="summary"><dt>멤버십 코드</dt><dd>${r.code}</dd><dt>이름</dt><dd>${esc(r.name)}</dd><dt>연락처</dt><dd>${esc(r.phone)}</dd><dt>접수 시각</dt><dd>${date(r.receivedAt)}.${String(new Date(r.receivedAt).getMilliseconds()).padStart(3,'0')}</dd><dt>접수 구분</dt><dd>${r.early?'조기 제출 · 정상 신청자 뒤 배정':'정상 접수'}</dd></dl><p class="small">접수 기록이 안전하게 기록되었습니다. PostgreSQL 반영은 별도로 진행됩니다.</p>`}
async function wizard(id){const d=await api('/forms/'+id);if(d.mine){location.hash='form/'+id;return}const t=await api('/forms/'+id+'/ticket','POST');let step=0;const key='draft:'+id;let draft={name:'',phone:''};try{draft=JSON.parse(sessionStorage.getItem(key))||draft}catch{}const render=()=>{app.innerHTML=`<div class="wizard"><div class="actions"><a class="back" href="#form/${id}">← ${esc(d.form.title)}</a><span class="muted">${step+1} / 4</span></div><div class="progress"><div style="width:${(step+1)*25}%"></div></div><form id="answer"><div class="step">${step===0?`<p class="muted">자동 발급 · 수정 불가</p><h2>팬클럽 멤버십 번호 (6자리)</h2><input readonly value="${t.code}" aria-label="멤버십 코드"><p class="small">이 폼림픽 안에서 중복되지 않는 영문 코드입니다.</p>`:step===1?`<h2>연습용 이름 *</h2><div class="notice">절대 실제 개인정보를 적지 마세요. 가상의 이름을 입력하세요.</div><input name="name" maxlength="40" required value="${esc(draft.name)}" placeholder="예: 연습하는토끼" autocomplete="off">`:step===2?`<h2>연습용 연락처 *</h2><div class="notice">절대 실제 개인정보를 적지 마세요. 실제 전화번호 대신 가상 값을 입력하세요.</div><input name="phone" maxlength="30" required value="${esc(draft.phone)}" placeholder="예: TEST-0001" autocomplete="off">`:`<h2>제출 준비가 되었어요.</h2><dl class="summary"><dt>멤버십 코드</dt><dd>${t.code}</dd><dt>연습용 이름</dt><dd>${esc(draft.name)}</dd><dt>연습용 연락처</dt><dd>${esc(draft.phone)}</dd></dl><p>신청 시작 ${date(d.form.startsAt)}</p><div class="clock" id="liveclock"></div><div class="notice">시계는 참고용입니다. 시작 전 제출하면 정상 신청자 뒤에 배정되며, 제출 후 수정할 수 없습니다.</div>`}</div><div class="actions"><button type="button" class="secondary" id="prev">${step?'이전':'닫기'}</button><button id="next">${step===3?'제출':'다음'}</button></div></form></div>`;document.querySelector('#prev').onclick=()=>{save();if(step){step--;render()}else location.hash='form/'+id};document.querySelector('#answer').onsubmit=async e=>{e.preventDefault();save();if(step<3){step++;render();return}e.submitter.disabled=true;try{await api('/forms/'+id+'/submissions','POST',draft);sessionStorage.removeItem(key);toast('접수가 완료되었습니다.');location.hash='form/'+id}catch(x){toast(x.message+' 동일한 제출을 다시 시도해도 중복 접수되지 않습니다.');e.submitter.disabled=false}}};function save(){for(const k of ['name','phone']){const el=document.querySelector(`[name="${k}"]`);if(el)draft[k]=el.value}sessionStorage.setItem(key,JSON.stringify(draft))}render();timer=setInterval(()=>{const el=document.querySelector('#liveclock');if(el)el.textContent=time(now())},50)}
window.addEventListener('hashchange',route);route();
