# Auto-detect text files and normalize line endings to LF
* text=auto eol=lf

# Explicitly enforce LF for shell scripts
*.sh text eol=lf
gradlew text eol=lf

# Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf

*.jar binary
